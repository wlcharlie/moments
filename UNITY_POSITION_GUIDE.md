# Unity 定位數值說明指南

## 📐 座標系統基礎

Unity 使用 **世界座標系統（World Space）** 來定位 GameObject。這是一個三維座標系統：

```
         Y (上)
         ↑
         |
         |
    ←────┼────→ X (右)
         |
         |
         ↓
        Z (深度/前後)
```

### Transform Position 的三個數值

```
{x: 0, y: 2, z: 0}
 │    │    │
 │    │    └─ Z 軸：深度（2D 遊戲通常設為 0）
 │    └────── Y 軸：垂直位置
 └─────────── X 軸：水平位置
```

## 🎯 各軸的含義

### X 軸（左右方向）

- **X = 0**：螢幕正中央
- **X > 0**（正值）：向右移動
  - X = 3：在中心右側 3 個單位
  - X = 5：在中心右側 5 個單位（更右）
- **X < 0**（負值）：向左移動
  - X = -3：在中心左側 3 個單位
  - X = -5：在中心左側 5 個單位（更左）

```
螢幕視圖：
┌─────────────────────────────┐
│  ←  X = -5  X = -3          │
│           X = 0 (中心)       │
│                   X = 3  →  │
│                         X = 5│
└─────────────────────────────┘
```

### Y 軸（上下方向）

- **Y = 0**：螢幕水平中心
- **Y > 0**（正值）：向上移動
  - Y = 2：在中心上方 2 個單位
  - Y = 4：在中心上方 4 個單位（更高）
- **Y < 0**（負值）：向下移動
  - Y = -2：在中心下方 2 個單位
  - Y = -4：在中心下方 4 個單位（更低）

```
螢幕視圖：
┌─────────────────────────────┐
│        Y = 4 (高)           │
│        Y = 2                │
│        Y = 0 (中心)         │
│        Y = -2               │
│        Y = -4 (低)          │
└─────────────────────────────┘
```

### Z 軸（深度方向）

在 2D 遊戲中，Z 軸通常設為 **0**，用於控制前後層次：
- **Z 值越小**：越前面（優先顯示）
- **Z 值越大**：越後面（會被前面的物件遮住）

## 🎨 實際範例

### 目前的 ComicImage 設置

```csharp
ComicImage: {x: 0, y: 2, z: 0}
```
- **X = 0**：在螢幕水平正中央
- **Y = 2**：在螢幕中心上方 2 個單位
- **Z = 0**：正常深度

視覺位置：
```
       ↑ Y = 2
       │
   [ComicImage]
       │
───────┼───────→ X = 0 (中心)
```

### 建議的新設置

**ComicImageLeft：**
```csharp
{x: -3, y: 2, z: 0}
```
- **X = -3**：在螢幕中心**左側** 3 個單位
- **Y = 2**：在螢幕中心上方 2 個單位（與原來的相同高度）
- **Z = 0**：正常深度

**ComicImageRight：**
```csharp
{x: 3, y: 2, z: 0}
```
- **X = 3**：在螢幕中心**右側** 3 個單位
- **Y = 2**：在螢幕中心上方 2 個單位（與原來的相同高度）
- **Z = 0**：正常深度

視覺位置：
```
        ↑ Y = 2
        │
[Left]  [Center]  [Right]
        │
────────┼────────→ X = 0
-3      0         3
```

## 🔧 如何將物件置於左邊

### 方法 1：直接設置 X 為負值

1. 選擇 GameObject（例如 `ComicImageLeft`）
2. 在 Inspector 中找到 **Transform** 組件
3. 找到 **Position** 欄位
4. 將 **X 值設為負數**，例如：
   - `X = -3`：中等左側位置
   - `X = -5`：更左側
   - `X = -2`：稍微左側

### 方法 2：在 Scene 視窗中拖動

1. 選擇 GameObject
2. 在 Scene 視窗中，看到移動工具（移動箭頭）
3. 拖動**紅色的 X 軸箭頭**向左移動
4. 觀察 Position 的 X 值會變成負數

### 調整建議

根據你想要的左側程度：

| X 值 | 位置描述 | 適用場景 |
|------|----------|----------|
| `-2` | 稍微偏左 | 接近中心，但略偏左 |
| `-3` | 明顯左側 | 左右對稱排列（推薦） |
| `-4` | 很左側 | 更明顯的分離 |
| `-5` | 極左側 | 需要更大空間時 |

## 📏 常見的螢幕範圍參考

假設使用標準的 Orthographic Camera（正交相機）：

- 螢幕寬度（X 軸範圍）：大約 `-6` 到 `+6`（取決於相機設置）
- 螢幕高度（Y 軸範圍）：大約 `-5` 到 `+5`（取決於相機設置）

**建議的左右位置：**
- 左側：X = -3 到 -4
- 中心：X = 0
- 右側：X = 3 到 4

## ✅ 快速檢查清單

要將物件放在左邊：

1. ✅ X 值必須是**負數**（-1, -2, -3...）
2. ✅ Y 值保持不變（如果要在同一水平線）
3. ✅ Z 值通常設為 0（2D 遊戲）

**範例：**
- ❌ `{x: 3, y: 2, z: 0}` → 這是在右邊！
- ✅ `{x: -3, y: 2, z: 0}` → 這是在左邊！

## 💡 實際操作步驟

1. **選擇 GameObject**
   - 在 Hierarchy 中點擊 `ComicImageLeft`

2. **查看 Transform 組件**
   - 在 Inspector 面板中應該可以看到 Transform

3. **修改 Position 的 X 值**
   ```
   Position
   ├─ X: -3  ← 改成負數
   ├─ Y: 2
   └─ Z: 0
   ```

4. **觀察效果**
   - 在 Scene 視窗中可以看到物件移動到左側
   - 數值越負（-5, -6...），位置越左

## 🎯 總結

**關鍵記住：**
- **X < 0**（負數）= 左邊 ✅
- **X = 0** = 正中央
- **X > 0**（正數）= 右邊

**要放在左邊，只要把 X 值改成負數就可以了！**

